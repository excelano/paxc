/target
*.zip

# Round-trip test corpus: real PA flow exports from tenants. NEVER commit
# -- the JSON contains tenant identifiers, user IDs, SharePoint URLs, and
# internal flow names. The test harness in tests/decoder.rs reads this
# directory via glob; if empty, the corpus test passes vacuously, so the
# harness keeps working with or without local fixtures.
tests/corpus/
